package ExceptionHandling;

// Unchecked Exceptions (Runtime exceptions): These are not checked at compile time. Examples include ArithmeticException, NullPointerException, etc.

public class UncheckedException {
        public static void main(String[] args) {
            int result = 10 / 0; // Will throw ArithmeticException
        }
}

